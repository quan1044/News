<%- include ('../layouts/head.ejs') %>


<body>

    <div id="wrapper">

        <%- include ('../layouts/navigation.ejs') %>

        <!-- Page Content -->
        <div id="page-wrapper">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">Loại tin
                            <small>Sửa<a href=""></a></small>
                        </h1>
                    </div>
                    <!-- /.col-lg-12 -->
                    <div class="col-lg-7" style="padding-bottom:120px">
                        <% if (locals.errors) { %>
                            <% if (errors!=-1) { %>
                                <% for(var i=0; i<errors.length; i++) {%>
                                    <div class="col-lg-12 alert alert-danger" role="alert">
                                        <%= errors[i].msg %> 
                                    </div>
                                <% } %>
                            <% } %>
                        <% } %>
                        <form action="admin/loaitin/sua" method="POST" id="form">
                            <div class="form-group">
                                <label>Thể loại<span style="color: red;">*</span></label>
                                <select class="form-control" name="theloai">
                                    <option value=0>Mời bạn chọn thể loại</option>
                                    <% lstTheloai.forEach(item => { %>
                                        <option value=<%=item.id %>><%=item.Ten %> </option>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="form-group">
                                <label>Tên loại tin<span style="color: red;">*</span></label>
                                <input id='txtName' class="form-control" name="loaitin"
                                    placeholder="Mời bạn nhập tên loại tin" />
                            </div>
                            <button type="submit" class="btn btn-default" id="btnSubmit">Sửa</button>
                            <button type="reset" class="btn btn-default">Làm mới</button>
                            <form>
                    </div>
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container-fluid -->
        </div>
        <!-- /#page-wrapper -->

    </div>
    <!-- /#wrapper -->

    <%- include ('../layouts/script.ejs') %>

    <script>
        $(document).ready(function () {
            $("#btnSubmit").click(function () {
                let id ='<%= id%>'
                $('#form').append("<input type='hidden' name='id' value='"+
                         id+"' />");
            });
        });
    </script>
</body>

</html>